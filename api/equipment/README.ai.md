### AI Hints for :api:equipment

This module adds endpoint-level Equipment v2 DTOs while reusing core installation, gateway, device, and enum models from `:api:dto`.

- Pagination lives in `xyz.dussim.viessmann.api.models.ResponseData`.
- Status payloads here intentionally model the endpoint responses, not the full equipment domain.
