### AI Hints for :api:auth

This module contains transport DTOs for auth validation, SAML, and CSRF endpoints.

- Keep `x-uuid` mapped with `@SerialName`.
- SAML-specific error payloads are intentionally kept separate from the shared generic error DTO.
