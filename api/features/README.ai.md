### AI Hints for :api:features

This module is limited to HTTP-layer DTOs for Features v2.

- Core feature objects live in `:api:feature:common`.
- Command execution responses here wrap the transport payload returned by the API.
