### AI Hints for :api:users

This module focuses on registration and address validation payloads.

- `UserType` mirrors the existing sealed enum style used in `:api:dto`.
- Some nested response objects are intentionally partial because the upstream schema is loose.
